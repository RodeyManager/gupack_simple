;(function() {
    'use strict';

    let App = {
        // 接口host
        ServerHost: 'api/',
        method: 'POST',
        sendTime: 60
    };

    this['App'] = App;
    return App;

}).call(this || window);
